/*
// import ModelDataSource, { GraphModelResultDataSource } from "./DataSource/modelDataSource";

// Ordered alfabetically to facilitate maintenance

export const DOWNLOAD_ALARM_HISTORY_RESULTS = 'DOWNLOAD_ALARM_HISTORY_RESULTS';

// A Mailbox is a collection of API calls under a commom "mailbox" name.
// This maps requests to MAILBOX_NAME to a set of API calls.
// Ordered alfabetically to facilitate maintenance
export default class Fedx {
  build(hub) {
    hub.attachMailbox(
      DOWNLOAD_ALARM_HISTORY_RESULTS,
      new AlarmHistoryDataSource().download
    );
  }
}

*/
